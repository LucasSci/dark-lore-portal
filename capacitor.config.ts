import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.arquivodocontinente.portal',
  appName: 'Arquivo do Continente',
  webDir: 'dist',
};

export default config;
